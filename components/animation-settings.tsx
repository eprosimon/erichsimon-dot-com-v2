"use client"

// This component has been deprecated as animations are now automatically
// enabled or disabled based on the current page path.
// Animations are enabled only for project, review, and recommendation pages.

export function AnimationSettings() {
  return null;
}

